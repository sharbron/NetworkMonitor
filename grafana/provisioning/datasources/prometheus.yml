apiVersion: 1
datasources:
  - name: Prometheus
    uid: prometheus           # Fixed UID so dashboard JSON references work reliably
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: "30s"
      httpMethod: POST
